const web3 = new Web3(ganache.provider());

const { interface, bytecode } = require("../compile");

let accounts;
let inbox;

beforeEach(async () => {
  try {
  } catch (error) {
    console.log(error);
  }
});

describe("Lottery", () => {});
